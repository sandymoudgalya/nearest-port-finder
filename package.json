{
  "name": "nearest-port-finder",
  "version": "1.0.0",
  "description": "Find the nearest ports to your location with a premium web UI",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}